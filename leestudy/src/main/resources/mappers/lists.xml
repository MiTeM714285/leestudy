<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.springboot.leestudy.domain.lists.ListsRepository">
	<select id="getUniversityListAll" resultType="com.springboot.leestudy.domain.lists.Entity.ListUniversity">
		select 
		university_code, university_name, university_addr1, university_addr2, university_type, university_campus, university_foundation 
		from list_university
		order by university_name
	</select>
	
	<select id="getSubjectCategoryListAll" resultType="String">
		SELECT distinct subject_category FROM list_subject order by subject_category
	</select>
	
	<select id="getSubjectNameListBySubjectCategory" parameterType="String" resultType="String">
		SELECT subject_name FROM list_subject where subject_category = #{subject_category} order by subject_name
	</select>
	
	<select id="getAddressPart1ListAll" resultType="String">
		SELECT distinct address_part1 FROM list_address order by address_part1
	</select>
	
	<select id="getAddressPart2ListByAddressPart1" parameterType="String" resultType="String">
		SELECT address_part2 FROM list_address where address_part1 = #{address_part1} order by address_part2
	</select>

	<select id="getPersonalityNameListAll" resultType="String">
		SELECT personality_name FROM list_personality order by personality_name
	</select>
</mapper>